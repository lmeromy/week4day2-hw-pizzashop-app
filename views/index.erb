<h1> Pizza for all! </h1>

<%# loops thru @orders array and calls pretty_name method on each element
don't forget the = in the icecream cone for printing! %>
<%# erb comment %>
<%for order in @orders %>
  <p>  Name: <%= order.pretty_name() %> </p>
  <p>  Order: <%= order.quantity %> <%= order.topping()%> pizza(s)</p>
  <p><a href="pizza-orders/<%= order.id%>">Show Order</a></p>
<% end %>
